\section{Database Aggregation}

In order to conduct a comprehensive analysis of the data acquired during the data acquisition phase, and to gain a thorough understanding of the other students' findings, I aggregated my database with the databases of 39 other students, resulting in a combined dataset of 40 databases. The following table lists the student IDs of the databases included in this aggregation:

\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{ccccc}
23025313 & 23025328 & 23041405 & 23047806 & 23071063 \\
23071082 & 23079239 & 23079506 & 23080363 & 23082320 \\
23084716 & 23100834 & 23123639 & 23129103 & 23129118 \\
23129194 & 23135689 & 23148157 & 23158572 & 23158587 \\
23173040 & 23206422 & 23220843 & 23221189 & 23240175 \\
23262136 & 23273412 & 23277555 & 23293505 & 23293539 \\
23308310 & 23359384 & 23365366 & 23453618 & 23455702 \\
23514826 & 23711815 & 23726011 & 23727550 &  \\
\end{tabular}
\end{center}

Since we all followed a shared schema, the aggregation process went smoothly. However, several challenges did arise, which I discuss in the following subsections.

\subsection{Deduplication}

The 40 source databases were built independently by different students, each pulling from the same pool of public data repositories (Zenodo, Dryad, Harvard Dataverse, ICPSR, and others). Because several students' searches overlapped, many of the same underlying research projects were captured more than once across the collected databases — sometimes with slightly different metadata, since each student's ingestion process handled things a little differently.

To resolve this, every project record was checked against the ones already merged, using a three-tier matching strategy applied in order of reliability: first by its project URL (normalized for trivial formatting differences like a trailing slash or letter case), then, if no URL was available or matched, by its DOI, and finally, if neither was usable, by the combination of title and source repository. This fallback chain was necessary because metadata completeness varied across sources — not every record had a DOI, and not every URL was formatted consistently.

When two records were identified as the same project, they were not simply deduplicated by discarding one copy. Instead, their information was combined: any field one copy was missing (e.g. a description or language) was filled in from the other if available, and all associated files, keywords, authors, and license entries from both copies were merged into a single unified set, with exact repeats (e.g. the same file name appearing in both) collapsed into one entry.

The result is a single database in which each real-world project appears exactly once, retaining the most complete metadata available across all the sources that happened to include it.

\subsection{Data Quality Remediation}

After aggregating multiple student databases into a single database following a shared class schema, a various inconsistencies appeared due to low quality data of some of those databases. Because each database was built separately, values that should conform to the same fixed categories were recorded inconsistently across sources. When the merged database was validated against the required schema---specifically, mandatory fields and the defined value sets for \textit{download method}, \textit{file download status}, and \textit{contributor role}---a substantial number of records failed validation.

\subsubsection{Issues Identified}
Validation surfaced five categories of problems:
\begin{itemize}
    \item A small number of records (4) contained no genuine project data---placeholder or test entries with no identifying title, URL, or DOI.
    \item 711 projects were missing their canonical project URL, despite having a valid DOI recorded elsewhere on the same record.
    \item Approximately 49{,}000 records used non-standard labels for \textit{download method} (e.g., ``API'' and ``api'' instead of the defined category), reflecting differences in how individual source databases recorded this field rather than genuinely new categories.
    \item Approximately 63{,}000 file records used non-standard status labels for the same reason.
    \item A small number of contributor-role entries (1{,}188) used inconsistent capitalization, 57 records were missing a required timestamp, and one record was found to be missing its description entirely.
\end{itemize}

\subsubsection{Resolution Approach}
Each issue was addressed according to its underlying cause rather than through a uniform rule. Records with no genuine identifying information were removed. Where a valid alternative identifier existed on the record (a DOI), it was used to populate the missing URL field. Where the same underlying value had simply been recorded under different labels across sources, these were consolidated to the schema's defined categories. Where a required value was genuinely absent and could not be recovered from elsewhere in the record, a clearly identifiable placeholder was applied so the gap remains traceable rather than being disguised as genuine data. The single missing description is being completed manually, as it requires domain judgment rather than a mechanical fix.


\subsection{Final Database}

The final aggregated database contains 89{,}658 unique projects collected from the 40 merged student databases. Together, these projects are associated with over one million files, approximately 866{,}000 keywords, more than 250{,}000 contributor-role assignments, and over 82{,}000 license records. Following deduplication and data quality remediation, the database conforms to the shared schema and serves as the foundation for the analyses presented in the remainder of this report.

\begin{table}[ht]
\centering
\caption{Size of the final aggregated database.}
\label{tab:aggregated-dataset}
\begin{tabular}{lr}
\toprule
\textbf{Entity} & \textbf{Count} \\
\midrule
Projects            & 89{,}658 \\
Files               & 1{,}005{,}361 \\
Keywords            & 866{,}212 \\
Contributor Roles   & 250{,}373 \\
Licenses            & 82{,}441 \\
\bottomrule
\end{tabular}
\end{table}

Since the analysis is conducted on a per-repository basis, it is essential to identify which repositories are included in the study and to understand how they are distributed across the aggregated database. In particular, examining the frequency distribution of repositories provides important context for interpreting the results, as it highlights potential imbalances in representation and ensures that conclusions drawn from the analysis are properly grounded in the underlying data structure.

\begin{table}[ht]
\centering
\caption{Distribution of projects by source repository in the aggregated database.}
\label{tab:repository-distribution}
\begin{tabular}{lr}
\toprule
\textbf{Repository} & \textbf{Projects} \\
\midrule
1. Zenodo                                   & 51{,}426 \\
10. Harvard Dataverse                        & 12{,}921 \\
5. DANS                                     & 6{,}973 \\
2. Dryad                                    & 4{,}652 \\
16. Open Data Uni Halle                      & 3{,}372 \\
11. Finnish Social Science Data Archive      & 2{,}969 \\
3. UK Data Service                          & 1{,}530 \\
4. Syracuse Qualitative Data Repository     & 1{,}286 \\
20. SIKT                                     & 950 \\
13. CESSDA                                   & 648 \\
15. ICPSR                                    & 530 \\
7. ADA                                      & 472 \\
18. Harvard Murray Archive                   & 422 \\
6. DataverseNO                              & 414 \\
19. Columbia Oral History Archive            & 327 \\
9. HIHSN                                    & 322 \\
8. SADA                                     & 225 \\
17. N/A                                      & 125 \\
12. AUSSDA                                   & 94 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
    \centering
    \includesvg[width=10cm]{res/repository_pie} 
    \caption{Pie chart of top 7 most common repositories in the aggregated database.}
    \label{fig:repo_pie}
\end{figure}

\begin{figure}[H]
    \centering
    \includesvg[width=10cm]{res/repository_histogram} 
    \caption{Histogram of project distributions per repository}
    \label{fig:repo_histogram}
\end{figure}